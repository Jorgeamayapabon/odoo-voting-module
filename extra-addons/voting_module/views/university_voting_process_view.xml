views_voting_process = '''
<odoo>
    <record id="view_university_voting_process_form" model="ir.ui.view">
        <field name="name">university.voting_process.form</field>
        <field name="model">university.voting_process</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="start_date"/>
                        <field name="end_date"/>
                        <field name="state"/>
                        <field name="candidates" widget="many2many_tags"/>
                    </group>
                    <notebook>
                        <page string="Votos">
                            <field name="votes">
                                <tree>
                                    <field name="student_id"/>
                                    <field name="candidate_id"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_university_voting_process_tree" model="ir.ui.view">
        <field name="name">university.voting_process.tree</field>
        <field name="model">university.voting_process</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="start_date"/>
                <field name="end_date"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <menuitem id="menu_university_voting_process" name="Voting process" parent="base.menu_custom" action="action_university_voting_process"/>
</odoo>
'''
